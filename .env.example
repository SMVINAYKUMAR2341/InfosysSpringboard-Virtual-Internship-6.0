# =============================================================================
# Environment Variables Template
# =============================================================================
# Copy this file to .env and replace placeholder values with your actual secrets
# NEVER commit .env file to version control
#
# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Replace all placeholder values below with your actual credentials
# 3. Keep .env file secure and never commit it to Git
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection string with asyncpg driver
# Format: postgresql+asyncpg://username:password@host:port/database
# Replace YOUR_PASSWORD with your actual PostgreSQL password
DATABASE_URL=postgresql+asyncpg://postgres:YOUR_PASSWORD@localhost/loan_app_db

# -----------------------------------------------------------------------------
# JWT Secret Key
# -----------------------------------------------------------------------------
# Used for signing JWT authentication tokens
# MUST be at least 32 characters long
# Generate a secure key with: openssl rand -hex 32
# NEVER use the example below in production - generate your own!
JWT_SECRET_KEY=your-generated-secret-key-here-minimum-32-characters-required

# -----------------------------------------------------------------------------
# OpenRouter API Key
# -----------------------------------------------------------------------------
# API key for accessing OpenRouter's LLM services
# Get your API key from: https://openrouter.ai/
# Format: sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key-here

# =============================================================================
# Security Best Practices:
# =============================================================================
# - Use strong, randomly generated secrets (especially JWT_SECRET_KEY)
# - Never commit actual secrets to Git
# - Use different credentials for development and production
# - Rotate credentials regularly (at least every 90 days)
# - Keep the .env file with restricted permissions (chmod 600 .env)
# - Never share your .env file or credentials with others
# =============================================================================
